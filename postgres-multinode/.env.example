# =============================================================================
# Shared passwords - MUST be identical across ALL nodes
# =============================================================================
# Generate with: ./scripts/generate-passwords.sh
APP_PASSWORD=
ADMIN_PASSWORD=
PGEDGE_PASSWORD=
PGCAT_AUTH_PASSWORD=
PGCAT_ADMIN_PASSWORD=

#!/bin/bash
# RedisInsight has no required env vars
# Database connections are configured in the RedisInsight UI after deploy

cat << EOF
# RedisInsight
# No env vars required — configure Redis connections in the UI
# Access: https://redis.example.com
EOF
